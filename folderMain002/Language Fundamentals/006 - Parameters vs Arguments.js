/* ----- Parameters vs Arguments ----- */

/** -- Parameters -- **/
// Parameters are these variables which you specify between parantheses when defining a function

// Example
// name is a parameter
function greeting(name) {
    // do something
    console.log(name);
}

/** -- Arguments -- **/
// Arguments are the concrete values you pass to a function when calling that function

// Example
greeting('Lucas');
