/* ----- NodeList and HTMLCollection ----- */

/*** NodeList ***/
// querySelectorAll() returns a static (not live) NodeList representing
// a list of document's elements that match the specified group of selectors

/*** HTMLCollection ***/
// getElementsByClassName() return a live HTMLCollections representing an
// array-like object of all child elements which have all of the given class name(s)
